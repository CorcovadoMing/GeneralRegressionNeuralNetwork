#pragma once

#include <vector>

const double activator(const double, const double, const double);
const double grnn(const double, const std::vector<double>, const std::vector<double>, const double);
